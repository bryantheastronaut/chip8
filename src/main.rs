// import mod that handles opcode / cpu stuff
pub mod chip;

fn main() {
    println!("Hello, world!");
    // setup graphics, window, size, etc
    // bind input system
    // initialize the opcode system
    // load game
    chip::testing();
    'game : loop {
        'event : loop {

        }
    }

}
